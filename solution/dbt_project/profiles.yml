dbt_project:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: mews_user
      password: postgres_password
      port: 5433
      dbname: mews-data-task-db
      schema: dev
      